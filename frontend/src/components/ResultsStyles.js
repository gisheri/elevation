import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  paper: {
    margin: '1rem',
    width: '30rem',
    padding: '1rem',
    maxHeight: '600px',
    overflow: 'scroll'
  }
});

export { useStyles };
