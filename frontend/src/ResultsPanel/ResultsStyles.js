import { makeStyles } from '../utils/index';

const useStyles = makeStyles({
  paper: {
    margin: '1rem',
    width: '30rem',
    padding: '1rem',
    maxHeight: '600px',
    minWidth: 395,
    overflow: 'scroll'
  }
});

export { useStyles };
